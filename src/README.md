# Rules

1. Blocks that requires any JS/TS that would otherwise be in a `<script></script>` tag needs to be in a vue component
2. Custom types will all be written in CamelCase: `interface ExampleType {}`
3. **KEEP. IT. SIMPLE. STUPID.**
