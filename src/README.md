# Rules

1. All code that has the import call `import { app } from '@firebase/server'` MUST be an API route
2. Any section that requires any JS/TS that would otherwise be in a `<script></script>` tag needs to be in a vue component
3. **KEEP. IT. SIMPLE. STUPID.**
